# Sewitt landing (Next.js)

## Scripts

- `pnpm dev` — local dev server
- `pnpm build` — production build
- `pnpm start` — run the production server (after `pnpm build`)

## Stack

Next.js App Router, React 19, Tailwind CSS v4, TypeScript.
